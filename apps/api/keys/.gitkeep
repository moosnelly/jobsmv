# This file ensures the keys directory structure is preserved in git
# JWT key files (*.pem, *.key, *.p12, *.pfx) are excluded via .gitignore
# Keys will be auto-generated on application startup if they don't exist
