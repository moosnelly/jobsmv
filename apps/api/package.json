{
  "name": "@jobsmv/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'FastAPI backend - no build step'",
    "lint": "ruff check .",
    "type-check": "mypy .",
    "format": "ruff format .",
    "test": "pytest",
    "migrate": "alembic upgrade head",
    "migrate:create": "alembic revision --autogenerate -m"
  }
}

